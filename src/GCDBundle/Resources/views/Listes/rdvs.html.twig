{% extends "GCDBundle::layout.html.twig" %}

{%block container %}
{% if app.user.username is defined %}      
   <div id="utilisateur">
         <div class="deconnexion">
             
             <a href="{{ path('gcd_deconnexion') }}">
                 <span> Déconnexion</span>
                 <img class="img-responsive decon-img" src="{{ asset('bundles/gcd/images/deconnexion.png') }}"/> 
             </a>
            </div>
              <div class="profile">
         
               <a href="{{ path('gcd_compte') }}">
            <span> {{ app.user.username }} </span>
         <img class="img-responsive profile-img" src="{{ asset('bundles/gcd/images/profile.png') }}"/> 

          
        </a>
        </div>
        </div>
         {% endif %} 
 

         <h1 class="title">Liste des rendez-vous</h1>
     
    {% for r in rdv %}

        <div>
         <div  style="display:inline-block;color: white;
  margin: 2px auto;
  border: 2px solid #fff;
  min-height: 50px;
  max-width: 500px;
  padding: 10px;"> 
                {% for p in patient %}
                {% if p.idPatient == r.idPatient%}
                {{ p.nomPatient }} {{ p.prenomPatient }} 
                {% endif %}
                {% endfor %}
                
                
                | {{ r.horraireRdv|date }}</div>
                <div style="display:inline-block;color: white;
  margin: 2px auto;
  border: 2px solid #fff;
  min-height: 50px;
  max-width: 500px;
  padding: 10px;">
                   <a href="{{ path('gcd_modifierCabinet',{ id:r.idRdv})}}">
                          <img class="img-responsive decon-img" src="{{ asset('bundles/gcd/images/fiche.png') }}"/>
                    </a>
                               
                    <a href="{{ path('gcd_suppCabinet',{ id:r.idRdv})}}">

                            <img class="img-responsive decon-img" src="{{ asset('bundles/gcd/images/supp.png') }}"/>

                   </a>
                </div>
        </div>
                
              
    
        

    {%endfor%}
     
      
</tbody>

{% endblock %}